# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# These are exposed to the browser (prefixed with VITE_)

# Supabase Configuration
# Get these values from your Supabase project settings at https://app.supabase.com
# Settings -> API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Application URL (REQUIRED for production)
# Set this to your production domain for Paystack billing redirects
# In development, you can omit this to use localhost (with test keys only)
# Example: https://app.dttracker.com
VITE_APP_URL=

# =============================================================================
# SUPABASE EDGE FUNCTIONS ENVIRONMENT VARIABLES
# =============================================================================
# These must be set in Supabase Dashboard -> Edge Functions -> Settings
# They are server-side secrets and should NEVER be in frontend code

# OpenAI API Key (for Creator Scraper AI extraction)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# PAYSTACK BILLING (REQUIRED for subscriptions)
# -----------------------------------------------------------------------------
# Get your keys from: https://dashboard.paystack.com -> Settings -> API Keys

# IMPORTANT: Use TEST keys for development, LIVE keys for production
# Test keys start with: sk_test_...
# Live keys start with: sk_live_...
# PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxx

# Webhook secret for verifying Paystack webhook signatures
# Found in Paystack Dashboard -> Settings -> API Keys & Webhooks
# PAYSTACK_WEBHOOK_SECRET=your-webhook-secret

# Application URL for billing callbacks (REQUIRED for production)
# Must match your verified domain in Paystack Dashboard
# APP_URL=https://app.dttracker.com

# Wallet funding uses the same PAYSTACK_SECRET_KEY and APP_URL (or VITE_APP_URL).
# Success callback URL is /wallet?fund=success (user is redirected there after Paystack payment).

# Currency settings (optional, defaults to USD)
# BILLING_CURRENCY=USD
# PAYSTACK_CURRENCY=USD
# PAYSTACK_CURRENCY_RATE=1

# -----------------------------------------------------------------------------
# DOBBLETAP INTEGRATION (REQUIRED for webhook endpoints)
# -----------------------------------------------------------------------------
# API Key for authenticating incoming webhooks from Dobbletap
# This should match the API key configured in Dobbletap system
# SYNC_API_KEY=your-sync-api-key-here

# Dobbletap API endpoint for outbound sync operations (DTTracker -> Dobbletap)
# Example: https://qetwrowpllnkucyxoojp.supabase.co/functions/v1/make-server-8061e72e
# DOBBLE_TAP_API=https://your-dobbletap-project.supabase.co/functions/v1/make-server-8061e72e

# Note: The following variables are already set by Supabase:
# - SUPABASE_URL (your project URL)
# - SUPABASE_SERVICE_ROLE_KEY (for database access in edge functions)
